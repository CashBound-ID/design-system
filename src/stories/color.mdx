import { Meta } from '@storybook/blocks';

import DesignSystemProvider from '@/context/DesignSystem';

import Card from '@/components/Card';
import Grid from '@/components/Grid';
import Typograpy from '@/components/Typography';

import { DEFAULT_COLOR } from '@/constant/theme';

import CardShowcaseUI from '@/storybook/components/CardShowcaseUI';

<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,400;0,500;0,600;1,400;1,500;1,600&display=swap" rel="stylesheet" />
<link href="./assets/fonts/cashbound-icon/style.css" rel="stylesheet" />
 
<Meta title="Token/Colors" />

# Color

Our color principles built based on the recognition of cashbound brand colors to create not only consistent and usable.

### Retrieving Values
You can access the radius values using the `useDesignSystemProvider` hook:

```tsx
import { useDesignSystemProvider } from '@/context/DesignSystem';

const ColorExample = () => {
  const { color } } = useDesignSystemProvider();

  return (
    <div style={{ backgroundColor: color.VIOLET1200, padding: 16 }}>
      This box has a violet color.
    </div>
  );
};
```

### Color Palette
Our color palette totals 110 colors, including 9 primary colors and their derivative colors.
These colors can basically include the need for color in background applications design.

<DesignSystemProvider>
  #### **Common Color**
  <br />

   <Grid gap={24}>
    {['WHITE', 'BLACK'].map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${item}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>

  #### **Violet Color**
  <br />

   <Grid gap={24}>
    {new Array(12).fill(null).map((_, index) => {
      return (index + 1) * 100
    }).reduce((result, item) => {
      result.push(`VIOLET${item}`);
      return result
    }, []).map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${DEFAULT_COLOR[item]}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>

  #### **Green Cash Color**
  <br />

   <Grid gap={24}>
    {new Array(12).fill(null).map((_, index) => {
      return (index + 1) * 100
    }).reduce((result, item) => {
      result.push(`GREENCASH${item}`);
      return result
    }, []).map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${DEFAULT_COLOR[item]}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>

  #### **Gray Mauve Color**
  <br />

   <Grid gap={24}>
    {new Array(12).fill(null).map((_, index) => {
      return (index + 1) * 100
    }).reduce((result, item) => {
      result.push(`GRAYMAUVE${item}`);
      return result
    }, []).map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${DEFAULT_COLOR[item]}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>

  #### **Red Tomato Color**
  <br />

   <Grid gap={24}>
    {new Array(12).fill(null).map((_, index) => {
      return (index + 1) * 100
    }).reduce((result, item) => {
      result.push(`REDTOMATO${item}`);
      return result
    }, []).map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${DEFAULT_COLOR[item]}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>

  #### **Purple Color**
  <br />

   <Grid gap={24}>
    {new Array(12).fill(null).map((_, index) => {
      return (index + 1) * 100
    }).reduce((result, item) => {
      result.push(`PURPLE${item}`);
      return result
    }, []).map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${DEFAULT_COLOR[item]}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>

  #### **Blue Color**
  <br />

   <Grid gap={24}>
    {new Array(12).fill(null).map((_, index) => {
      return (index + 1) * 100
    }).reduce((result, item) => {
      result.push(`BLUE${item}`);
      return result
    }, []).map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${DEFAULT_COLOR[item]}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>

  #### **Green Grass Color**
  <br />

   <Grid gap={24}>
    {new Array(12).fill(null).map((_, index) => {
      return (index + 1) * 100
    }).reduce((result, item) => {
      result.push(`GREENGRASS${item}`);
      return result
    }, []).map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${DEFAULT_COLOR[item]}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>

  #### **Yellow Amber Color**
  <br />

   <Grid gap={24}>
    {new Array(12).fill(null).map((_, index) => {
      return (index + 1) * 100
    }).reduce((result, item) => {
      result.push(`YELLOWAMBER${item}`);
      return result
    }, []).map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${DEFAULT_COLOR[item]}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>

  #### **Orange Color**
  <br />

   <Grid gap={24}>
    {new Array(12).fill(null).map((_, index) => {
      return (index + 1) * 100
    }).reduce((result, item) => {
      result.push(`ORANGE${item}`);
      return result
    }, []).map((item) => {
      return (
        <Grid.Item col={2} key={item}>
          <CardShowcaseUI
            label={`<strong>${item}</strong>`}
            sourceCode={`const { color: { ${item} } } = useDesignSystemProvider();`}
          >
            <section style={{ width: 100, height: 100 }}>
              <Card
                shadow='xl'
                radius='md'
                background={`${DEFAULT_COLOR[item]}`}
                padding="48px"
              />
            </section>
          </CardShowcaseUI>
        </Grid.Item>
      )
    })}
  </Grid>
</DesignSystemProvider>
